## Components for Reszui

**Running the project:**
npm i
npm start

1. Custom Switch
   **Usage:**
   <CustomSwitch 
        initialState={true} 
        onChange={(state) => console.log("Switch is", state ? "On" : "Off")}
   />
